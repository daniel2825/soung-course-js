version: 1
frontend:
  phases:
    preBuild:
      commands:
      - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
      - nvm install 18
      - nvm use 18
    build:
      commands:
      - echo "No frontend to build"
  artifacts:
    baseDirectory: dummy
    files:
    - '**/*'
  cache:
    paths:
    - node_modules/**/*
