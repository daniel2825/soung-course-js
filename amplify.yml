version: 1
frontend:
  phases:
    preBuild:
      commands:
      - nvm install 16
      - nvm use 16
      - npm install
    build:
      commands:
      - echo "No frontend to build"
  artifacts:
    baseDirectory: dummy
    files:
    - '**/*'
  cache:
    paths:
    - node_modules/**/*

    version: 1.0
